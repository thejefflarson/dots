;;; jeffs-early-init --- another collection of bloops for emacs
;;; Commentary:
;; This file exists to tweak the gui before we boot
;;; Code:
(add-to-list 'default-frame-alist '(undecorated . t))
(provide 'early-init)
;;; early-init.el ends here
